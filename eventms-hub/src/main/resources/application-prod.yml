server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eventms?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

rabbitmq:
  host: localhost
  port: 5672

  username: guest
  password: guest

# Order timeout delay in milliseconds (5 minutes = 300000ms)
order:
  timeout:
    delay: 300000
